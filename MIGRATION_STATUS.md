# Статус миграции на PostgreSQL

## ✅ Что сделано

### 1. База данных
- ✅ Создана схема Prisma с моделями:
  - User (с полем phone и password)
  - UserSettings
  - Chat, ChatParticipant, Message
  - Post, Like, Repost, Comment
- ✅ Настроен Prisma Client
- ✅ Добавлены скрипты для миграций

### 2. Backend (Сервер)
- ✅ Обновлены маршруты аутентификации (phone вместо username)
- ✅ Добавлено хеширование паролей с bcrypt
- ✅ Переписаны маршруты постов на Prisma
- ✅ Переписаны маршруты чатов на Prisma
- ✅ Созданы маршруты настроек (/api/settings)
- ✅ Все данные теперь сохраняются в БД (не в памяти)

### 3. Frontend (Веб-приложение)
- ✅ Обновлена страница входа (phone вместо username)
- ✅ Обновлен authStore для работы с phone
- ✅ Страница настроек интегрирована с API
- ✅ Добавлено сохранение профиля, пароля и настроек

## ⚠️ Что нужно сделать

### 1. Установить и запустить PostgreSQL

**Вариант 1: Установка PostgreSQL**
1. Скачайте с https://www.postgresql.org/download/windows/
2. Установите (пароль: postgres)
3. Проверьте, что служба запущена

**Вариант 2: Docker (проще)**
```bash
docker run --name messenger-postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=messenger -p 5432:5432 -d postgres:15
```

### 2. Запустить миграции

После установки PostgreSQL:

```bash
cd apps/server
npm run prisma:generate
npm run prisma:migrate
```

Это создаст все таблицы в базе данных.

### 3. Запустить приложение

**Терминал 1 - Сервер:**
```bash
cd apps/server
npm run dev
```

**Терминал 2 - Веб-приложение:**
```bash
cd apps/web
npm run dev
```

### 4. Проверить работу

1. Откройте http://localhost:5173
2. Зарегистрируйтесь с номером телефона (например: +79991234567)
3. Проверьте, что:
   - ✅ Регистрация работает
   - ✅ Вход работает
   - ✅ Сообщения сохраняются
   - ✅ Посты сохраняются
   - ✅ Настройки сохраняются

## 📝 Изменения в API

### Аутентификация
- **Было:** `{ username, password }`
- **Стало:** `{ phone, password }`

### Новые эндпоинты
- `GET /api/settings` - получить настройки
- `PATCH /api/settings/profile` - обновить профиль
- `POST /api/settings/password` - изменить пароль
- `PATCH /api/settings/preferences` - обновить настройки

## 🔧 Полезные команды

```bash
# Просмотр данных в БД
cd apps/server
npm run prisma:studio

# Сброс БД (удалит все данные!)
cd apps/server
npx prisma migrate reset

# Проверка статуса PostgreSQL
# Windows: services.msc -> найти postgresql
# Docker: docker ps
```

## 📱 Формат номера телефона

Номер должен быть в международном формате:
- ✅ +79991234567
- ✅ +12025551234
- ❌ 89991234567
- ❌ 9991234567

## 🎯 Следующие шаги

После успешного запуска можно:
1. Добавить валидацию номера телефона на фронтенде
2. Добавить SMS-верификацию (Twilio, SMS.ru)
3. Реализовать изменение username в настройках
4. Добавить загрузку аватаров
5. Развернуть на VPS

## ❓ Проблемы?

Если база данных не подключается:
1. Проверьте, что PostgreSQL запущен
2. Проверьте DATABASE_URL в apps/server/.env
3. Попробуйте подключиться через pgAdmin или psql
