# Как запустить Desktop приложение

## Шаг 1: Убедись что сервер запущен

Открой **первый терминал** и запусти:
```bash
cd apps/server
npm run dev
```

Должно появиться: `🚀 Server running on port 3000`

## Шаг 2: Запусти Desktop приложение

Открой **второй терминал** и запусти:
```bash
cd apps/desktop
npm run "tauri dev"
```

**Важно:** Команда в кавычках `"tauri dev"` потому что в названии есть пробел!

## Альтернативный способ:

```bash
cd apps/desktop
npx tauri dev
```

## Если ошибка "tauri command not found":

Установи Tauri CLI глобально:
```bash
npm install -g @tauri-apps/cli
```

Потом запусти:
```bash
cd apps/desktop
tauri dev
```

## Проверка что всё работает:

1. ✅ Rust установлен: `rustc --version`
2. ✅ Node.js установлен: `node --version`
3. ✅ Сервер запущен на порту 3000
4. ✅ Desktop приложение откроется в отдельном окне

## Если приложение не открывается:

Посмотри ошибки в терминале и скопируй их мне.

Частые проблемы:
- **"Rust not found"** → Установи Rust: https://rustup.rs/
- **"Port 5174 already in use"** → Закрой другие Vite процессы
- **"Failed to fetch"** → Проверь что сервер запущен на порту 3000
