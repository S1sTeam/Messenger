# 🚀 Быстрый старт

## Шаг 1: Установите PostgreSQL

### Вариант A: Docker (Рекомендуется - проще всего)
```bash
docker run --name messenger-postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=messenger -p 5432:5432 -d postgres:15
```

### Вариант B: Установка PostgreSQL
1. Скачайте: https://www.postgresql.org/download/windows/
2. Установите с паролем: `postgres`
3. Убедитесь, что служба запущена

## Шаг 2: Запустите миграции

```bash
cd apps/server
npm run prisma:generate
npm run prisma:migrate
```

При запросе имени миграции введите: `init`

## Шаг 3: Запустите приложение

**Откройте 2 терминала:**

### Терминал 1 - Сервер
```bash
cd apps/server
npm run dev
```

Должно появиться: `🚀 Server running on port 3000`

### Терминал 2 - Веб-приложение
```bash
cd apps/web
npm run dev
```

Должно появиться: `Local: http://localhost:5173`

## Шаг 4: Откройте приложение

1. Откройте браузер: http://localhost:5173
2. Зарегистрируйтесь:
   - Имя: `Тестовый пользователь`
   - Телефон: `+79991234567`
   - Пароль: `test123`
3. Войдите в систему

## ✅ Проверьте работу

- [ ] Регистрация работает
- [ ] Вход работает
- [ ] Можно создавать посты
- [ ] Можно лайкать посты
- [ ] Можно отправлять сообщения
- [ ] Настройки сохраняются

## 🔧 Просмотр данных в БД

```bash
cd apps/server
npm run prisma:studio
```

Откроется http://localhost:5555 с интерфейсом для просмотра данных

## ❌ Если что-то не работает

### PostgreSQL не запускается
```bash
# Проверьте статус (Docker)
docker ps

# Перезапустите (Docker)
docker restart messenger-postgres

# Проверьте логи (Docker)
docker logs messenger-postgres
```

### Ошибка подключения к БД
Проверьте `apps/server/.env`:
```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/messenger
```

### Сервер не запускается
```bash
cd apps/server
npm install
npm run dev
```

### Веб-приложение не запускается
```bash
cd apps/web
npm install
npm run dev
```

## 📱 Тестовые данные

После регистрации создайте несколько постов и сообщений для проверки.

Формат телефона: `+[код страны][номер]`
- Россия: `+79991234567`
- США: `+12025551234`

## 🎉 Готово!

Теперь все данные сохраняются в PostgreSQL:
- ✅ Аккаунты
- ✅ Сообщения
- ✅ Посты
- ✅ Лайки и репосты
- ✅ Настройки пользователя

## 📚 Дополнительно

- `DATABASE_SETUP.md` - подробная инструкция по БД
- `MIGRATION_STATUS.md` - что было сделано
- Prisma Studio: http://localhost:5555 (после `npm run prisma:studio`)
